class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n=nums.size();
        int miss=(n*(n+1))/2;
        int sm=0;
        for(int i=0;i<n;i++){
            sm+=nums[i];
        }
        int miss_number=abs(miss-sm);
        return miss_number;
        
    }
};
